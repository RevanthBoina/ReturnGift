name: Build Debug APK

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v7

      - name: Set up JDK 17
        uses: actions/setup-java@v5.6.0
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: Setup Android SDK
        uses: android-actions/setup-android@v4

      - name: Cache Gradle packages
        uses: actions/cache@v6
        with:
          path: |
            ~/.gradle/caches
            ~/.gradle/wrapper
          key: gradle-${{ hashFiles('**/*.gradle*', '**/gradle-wrapper.properties') }}
          restore-keys: gradle-

      - name: Run unit tests
        run: ./gradlew testDebugUnitTest

      - name: Run wiring tests (C2/C3 regression guard)
        run: |
          echo "=== Running SkillRegistry wiring tests (C2: YAML skills loaded at startup) ==="
          ./gradlew testDebugUnitTest --tests "*SkillRegistryWiringTest" || exit 1
          echo "=== Running SafetyInterceptor wiring tests (C3: safety checks enforced) ==="
          ./gradlew testDebugUnitTest --tests "*SafetyInterceptorWiringTest" || exit 1

      - name: Run Android Lint
        run: ./gradlew lintDebug
        continue-on-error: true

      - name: Check for Detekt
        run: |
          if grep -q "id.*detekt" build.gradle.kts app/build.gradle.kts 2>/dev/null; then
            ./gradlew detekt
          else
            echo "Detekt not configured - skipping"
          fi

      - name: Build debug APK
        run: ./gradlew assembleDebug

      - name: Upload APK
        uses: actions/upload-artifact@v7
        with:
          name: debug-apk
          path: app/build/outputs/apk/debug/*.apk

      - name: Upload Lint Reports
        uses: actions/upload-artifact@v7
        if: always()
        with:
          name: lint-reports
          path: app/build/reports/lint-results-*.html
          retention-days: 7
          if-no-files-found: ignore
