# ReturnGift — Execution Plan

→ This file previously contained ReturnGift's acquisition-target execution plan. That content has been removed as it described a product ReturnGift is not building.

ReturnGift's project plan is in `BACKLOG.md` (feature backlog) and `README.md` (architecture direction).

## Current Phase: Foundation Hardening

Priority order:

1. **Harness safeguards (Parts A, B, C)** — mid-task interrupt handling, per-app allow-list, one-tap undo
2. **LoRA Action Skills infrastructure** — SkillOpt Skill doc format and loading pipeline
3. **Local model swap** — replace or disable cloud model paths, lock to on-device LiteRT-LM
4. **QA sweep** — full release-gate QA after safeguards land
